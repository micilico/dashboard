# Cloud Panel listener
CLOUD_PANEL_PORT=3130

# Mount path
CLOUD_PANEL_MOUNT_PATH=/mnt/ultra-media

# Rate limiting
CLOUD_PANEL_RATE_LIMIT_CALLS=40
CLOUD_PANEL_RATE_LIMIT_SECONDS=60

# CSRF
CLOUD_PANEL_CSRF_TOKEN_TTL_SECONDS=43200

# Quota seedbox ultra.cc — affiche votre quota réel dans la barre disque.
# Installer le script sur le slot : bash <(wget -qO- https://scripts.ultra.cc/util-v2/Ultra-API/main.sh)
# (option 1, puis option 4 pour réafficher le token). Backend uniquement, jamais exposé au frontend.
# URL = base (https://USER.HOST.usbx.me/ultra-api) ou endpoint complet (…/ultra-api/get-diskquota).
# La route réelle du script utilise un tiret (/get-diskquota), repli automatique sur /get_diskquota.
CLOUD_PANEL_ULTRA_API_URL=https://user.host.usbx.me/ultra-api
CLOUD_PANEL_ULTRA_API_TOKEN=change-me
CLOUD_PANEL_ULTRA_API_CACHE_TTL=60

# Sentry (optional)
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
