[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dashboard-common"
version = "0.1.0"
description = "Shared utilities for Dashboard panels"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["."]
